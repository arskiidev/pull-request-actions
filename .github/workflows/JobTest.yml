name: PR

on:
  pull_request:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  PR:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Get Runs
        run: |
          echo file from branch-1
          echo action from $(git rev-parse --abbrev-ref HEAD)